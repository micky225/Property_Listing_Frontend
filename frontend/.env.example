# Django API base (include /api, no trailing slash after api is OK either way —
# the client strips a trailing slash). Examples:
# Local:  http://127.0.0.1:8000/api
# Prod:   https://api.yourdomain.com/api
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
